#!/usr/bin/env sh
# Pre-commit: intentionally a no-op per user preference.
# All quality gates (lint, tsc, build) run on pre-push instead so commits
# stay fast and batch-friendly. Do not reintroduce checks here without
# explicit confirmation.

exit 0
